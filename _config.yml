title: 混成社
baseurl: "" # 如果你的 GitHub Repo 名稱不是 username.github.io，這裡要填入 /repo-name
url: ""

# 排除不需要編譯的檔案
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/

plugins:
  - jekyll-image-size
